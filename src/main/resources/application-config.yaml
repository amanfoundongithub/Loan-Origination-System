# Configuration yaml
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8081
          predicates:
            - Path=/users/**


###
# Actuator health check monitoring at /actuator/health
###
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
